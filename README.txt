IT Job Matcher - A semantic web application for matching IT graduates with jobs in Malaysia.
Built as part of the Semantic Web course (TSW).

To run:
  pip install -r requirements.txt
  cd api && python index.py
